# McStduio 二次开发手册

当前仓库是一个完整的 FreeCAD fork，现有功能体系已经比较完备。二次开发建议把它当作“成熟平台”来扩展，而不是优先改核心 CAD 内核。

## 当前状态

- 主体项目：FreeCAD 26.3.0 dev fork
- 技术栈：CMake、C++、Python、Qt/PySide6、OpenCASCADE、Coin3D
- 构建方式：`pixi.toml` 已封装依赖、配置、构建、安装、测试和启动任务
- 当前 debug 构建：`build/debug/bin/FreeCADCmd --version` 可运行，输出 `FreeCAD 26.3.0 Revision: 47377 (Git)`
- 当前 shell 问题：本机 shell 里暂时找不到 `pixi` 命令，需要先安装 pixi 或把 pixi 加入 `PATH`

## 推荐二次开发路线

优先顺序：

1. Python 工作台或插件
2. 内置 Python 模块
3. C++ App/Gui 模块
4. 品牌化发行与启动界面定制
5. 核心 `Base`、`App`、`Gui`、几何内核行为修改

前两种最适合“功能已经完整，只需要基于它增加业务能力”的场景，后两种才需要承担更多维护成本。

## 路线一：Python 工作台

适合：

- 加一组行业工具按钮、菜单、自动化命令
- 对现有 Part、Sketcher、Draft、TechDraw、CAM 等能力做组合调用
- 做业务流程封装，例如模板建模、参数化生成、批量导入导出、报表生成
- 希望未来容易同步 FreeCAD 上游代码

参考位置：

- `src/Tools/_TEMPLATEPY_`
- `src/Mod/TemplatePyMod`
- 真实模块可参考 `src/Mod/Draft`、`src/Mod/OpenSCAD`、`src/Mod/CAM`

核心文件职责：

- `Init.py`：非 GUI 初始化
- `InitGui.py`：注册工作台，声明图标、菜单、工具栏
- `<Module>_Gui.py` 或 `Commands.py`：注册命令，例如 `FreeCADGui.addCommand(...)`
- `Resources/icons/`：工作台和命令图标
- `CMakeLists.txt`：把 Python 文件和资源复制到构建目录与安装目录

建议做法：

1. 复制 `src/Tools/_TEMPLATEPY_` 为 `src/Mod/McStudio`
2. 把 `_TEMPLATEPY_` 统一替换为 `McStudio`
3. 在 `src/Mod/CMakeLists.txt` 增加 `add_subdirectory(McStudio)`
4. 先做一个能显示工作台、菜单和 Hello World 命令的最小版本
5. 后续命令内部调用 FreeCAD Python API，尽量不要直接改 `src/App` 或 `src/Gui`

## 路线二：内置 C++ 模块

适合：

- 需要新增高性能几何算法
- 需要新增 Document Object、Property、ViewProvider
- Python API 性能不够，或必须接入 C++ 库
- 需要同时提供 App 层对象和 Gui 层交互

参考位置：

- `src/Tools/_TEMPLATE_`
- `src/Mod/Part`
- `src/Mod/PartDesign`
- `src/Mod/Start`

核心结构：

- `App/`：数据对象、算法、Python binding、非 GUI 逻辑
- `Gui/`：命令、工作台、ViewProvider、Qt 界面
- `Init.py`：加载 App 模块
- `InitGui.py`：加载 Gui 模块
- `CMakeLists.txt`：分别注册 App 和 Gui 子目录

建议只在确实需要时走 C++ 模块路线。FreeCAD 核心大、编译慢、依赖重，C++ 改动的测试和维护成本明显高于 Python 工作台。

## 路线三：品牌化发行

适合：

- 把产品名从 FreeCAD 包装成 McStduio/McStudio
- 修改图标、启动图、README、安装包、桌面入口
- 内置默认工作台、默认偏好、示例文件

可关注位置：

- `version.json`：项目名和版本号来源
- `src/Gui/Icons/`：主图标资源
- `src/XDGData/`：Linux 桌面集成
- `package/WindowsInstaller/`：Windows 安装包资源
- `src/Mod/Start/`：启动页、最近文件、示例入口
- `data/examples/`：内置示例文件

注意：

- 不建议一开始大规模改 Python 模块名 `FreeCAD` 或 C++ namespace。这会破坏插件生态和脚本兼容性。
- 品牌化可以先从外层资源、安装包、启动页做起，核心 API 继续保持 FreeCAD 兼容。

## 不建议优先修改的区域

- `src/Base`：底层工具、参数、异常、文件系统、类型系统
- `src/App`：文档模型、Property 系统、事务、表达式、对象生命周期
- `src/Gui`：主窗口、命令系统、选择系统、3D 视图基础设施
- `src/3rdParty`：第三方库代码
- `src/Mod/Part`、`src/Mod/Sketcher`、`src/Mod/PartDesign` 的核心算法：除非目标就是 CAD 内核能力调整

这些区域可以改，但应该有明确目标、测试样例和回滚边界。否则后续同步上游时冲突会比较多。

## 常用构建命令

如果 `pixi` 可用：

```bash
cd /home/aa/Desktop/McStduio
pixi run configure-debug
pixi run build-debug
pixi run install-debug
pixi run freecad-debug
```

当前已有 debug 构建，也可以直接运行：

```bash
cd /home/aa/Desktop/McStduio
build/debug/bin/FreeCADCmd --version
build/debug/bin/FreeCAD
```

测试：

```bash
cd /home/aa/Desktop/McStduio
ctest --test-dir build/debug
```

只跑某类测试时，用 `-R` 过滤：

```bash
ctest --test-dir build/debug -R Start
ctest --test-dir build/debug -R Part
```

FreeCAD 自带测试入口：

```bash
build/debug/bin/FreeCADCmd --run-test 0
build/debug/bin/FreeCADCmd --run-test Part
```

## 建议的开发节奏

1. 先确定二次开发目标属于哪一类：业务工具、工作台、品牌化、底层算法。
2. 如果是业务功能，先新建 `src/Mod/McStudio` 工作台，不碰核心模块。
3. 每次只加一个命令或一个面板，跑通 GUI 注册、命令执行和基础测试。
4. 对稳定能力再考虑沉淀为 C++ 模块或公共 Python 包。
5. 品牌化和核心功能分支分开做，避免资源改名和功能改动混在一起。

## 最稳妥的首个任务

建议先做一个内置 `McStudio` Python 工作台：

- 出现在工作台下拉列表里
- 有自己的图标、菜单和工具栏
- 提供一个最小命令，例如创建一个参数化 Box 或打开指定模板文件
- 不修改 FreeCAD 核心功能

这个任务能验证构建链路、模块注册、资源加载和 FreeCAD Python API 调用，是后续二次开发的最低风险起点。
